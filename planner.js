const form=document.querySelector('#plan-form'),heading=document.querySelector('.result-heading h2'),caption=document.querySelector('#plan-caption'),result=document.querySelector('#plan-result'),detail=document.querySelector('#plan-detail'),blocks=document.querySelector('#block-list'),deferred=document.querySelector('#deferred-list');const esc=value=>String(value).replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[ch]));const dt=value=>new Date(value).toLocaleString();
const candidatePanel=document.createElement('section');candidatePanel.className='candidate-section';candidatePanel.innerHTML='<div class="section-heading compact-heading"><div><p class="eyebrow"><span></span> AUTOMATIC TASK POOL</p><h2>Tasks considered by the optimizer</h2></div><button class="outline-button" id="refresh-candidates" type="button">Refresh eligibility</button></div><p class="candidate-intro">Tasks are not manually selected: F-04 considers every F-01 task. A task is eligible only after F-02 and F-03 are complete; its COA window must start at the eligible F-02 assessment hour.</p><div class="candidate-table-wrap"><table class="candidate-table"><thead><tr><th>Task</th><th>Department</th><th>Section</th><th>F-03 score</th><th>Eligibility</th><th>Required COA start</th></tr></thead><tbody id="candidate-body"><tr><td colspan="6" class="empty-table">Loading recorded tasks…</td></tr></tbody></table></div>';document.querySelector('.planner-layout').before(candidatePanel);const candidateBody=document.querySelector('#candidate-body');
async function loadCandidates(){try{const response=await fetch('/api/v1/block-plans/eligibility'),items=await response.json();candidateBody.innerHTML=items.length?items.map(item=>`<tr><td><strong>${esc(item.task_id)}</strong><span class="candidate-reason">${esc(item.duration_minutes)} min</span></td><td>${esc(item.department)}</td><td>${esc(item.section_id)}</td><td>${item.priority_score===null?'—':esc(item.priority_score)}</td><td class="${item.eligible?'eligible-text':'ineligible-text'}">${item.eligible?'ELIGIBLE':'NOT READY'}${item.missing_requirements.length?`<span class="candidate-reason">${item.missing_requirements.map(esc).join(' ')}</span>`:''}</td><td>${item.required_coa_start_hour?esc(dt(item.required_coa_start_hour)):'—'}</td></tr>`).join(''):'<tr><td colspan="6" class="empty-table">No complete F-01 tasks found.</td></tr>'}catch{candidateBody.innerHTML='<tr><td colspan="6" class="empty-table">Could not load task eligibility.</td></tr>'}}document.querySelector('#refresh-candidates').addEventListener('click',loadCandidates);loadCandidates();
function isolation(task){const labels=[];if(task.requires_traffic_block)labels.push('Traffic block');if(task.requires_traction_disconnection)labels.push('Traction disconnection');return labels.join(' + ')||'None'}function renderPlan(plan){const m=plan.metrics;heading.textContent=`${m.scheduled_task_count} tasks · ${m.distinct_corridor_disruptions} blocks`;caption.textContent=`${plan.horizon} proposed plan · ${m.total_priority_score_scheduled} priority points scheduled · ${m.shared_block_count} shared blocks.`;result.className='result-data';result.innerHTML=`<span class="result-status complete">PROPOSED</span><dl><div><dt>Block hours used</dt><dd>${esc(m.total_block_hours_used)} h</dd></div><div><dt>Unscheduled tasks</dt><dd>${esc(m.unscheduled_task_count)}</dd></div><div><dt>Passenger trains affected</dt><dd>${esc(m.passenger_trains_affected)}</dd></div><div><dt>Goods trains affected</dt><dd>${esc(m.goods_trains_affected)}</dd></div></dl><div class="review-list">${plan.notes.map(esc).join('<br/>')}${m.search_timed_out?'<br/>Search time limit reached; review this proposed result before relying on it.':''}</div>`;detail.hidden=false;blocks.innerHTML=plan.scheduled_blocks.length?plan.scheduled_blocks.map(block=>`<article class="block-card"><div class="block-header"><div><strong>${esc(block.corridor_id)} · ${esc(block.section_id)}</strong><p>${esc(dt(block.window_start))} — ${esc(dt(block.window_end))}</p></div><span class="block-tag">${esc(block.assigned_tasks.length)} task${block.assigned_tasks.length===1?'':'s'} · ${esc(block.consolidated_departments.join(', '))}</span></div><div class="block-meta"><span>Timetable: ${esc(block.timetable_reference)}</span><span>Goods forecast: ${esc(block.goods_forecast_reference)}</span><span>Used: ${esc(block.used_minutes)} / ${esc(block.available_minutes)} min</span></div><table class="assignment-table"><thead><tr><th>Task</th><th>Department</th><th>Priority</th><th>Scheduled interval</th><th>Isolation</th></tr></thead><tbody>${block.assigned_tasks.map(task=>`<tr><td><strong>${esc(task.task_id)}</strong></td><td><i class="dept-dot"></i>${esc(task.department)}</td><td>${esc(task.priority_score)}</td><td>${esc(dt(task.scheduled_start))} – ${esc(dt(task.scheduled_end))}</td><td>${esc(isolation(task))}</td></tr>`).join('')}</tbody></table></article>`).join(''):'<p class="empty-table">No eligible task could be placed in the submitted COA windows.</p>';deferred.innerHTML=plan.unscheduled_tasks.length?plan.unscheduled_tasks.map(item=>`<article class="deferred-item"><strong>${esc(item.task_id)}</strong><p>${esc(item.reason)}</p></article>`).join(''):'<div class="deferred-item">No tasks were deferred.</div>'}
form.addEventListener('submit',async ev=>{ev.preventDefault();const d=new FormData(form);let windows;try{windows=JSON.parse(d.get('coa_windows'));if(!Array.isArray(windows))throw Error('COA windows must be a JSON array.')}catch(err){heading.textContent='Input needs correction';caption.textContent=err.message;return}const payload={horizon:d.get('horizon'),horizon_start:d.get('horizon_start'),horizon_end:d.get('horizon_end'),coa_windows:windows};try{const r=await fetch('/api/v1/block-plans',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),x=await r.json();if(!r.ok)throw Error(Array.isArray(x.detail)?x.detail.map(v=>v.msg).join('; '):x.detail||'Could not generate plan.');renderPlan(x)}catch(err){heading.textContent='Input needs correction';caption.textContent=err.message;result.className='empty-result';result.innerHTML='<p>No proposed plan was created.</p>';detail.hidden=true}});
